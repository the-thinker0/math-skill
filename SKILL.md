---
name: math-research-activator
description: |
  数学研究助手：将 AI 研究目标转成数学对象、可实现机制和可验证结论；用于架构/算子构造、理论分析、跨数学领域思路发现与结构迁移、研究相关数学查询，以及密码学定义、构造、归约与协议审查。纯实现型 debug、重构、调参和一般代码审查不触发。
  English: Turn AI research goals into mathematical formulations, implementable mechanisms, and testable conclusions. Use for architecture/operator construction, theoretical analysis, cross-field mathematical discovery, math-to-AI transfer, research-related mathematics, and cryptographic definitions, constructions, reductions, or protocol reviews. Exclude implementation-only debugging, refactoring, tuning, and general code review.
---

# 数学研究：从问题到构造与验证

本文件是 Claude Code / Codex / Cursor / DeepSeek Harness 的权威入口。按用户自然语言主框架回答；技术词、代码、路径、公式不决定语言。`SKILL.en.md` 仅供显式英文兼容入口，两份正文不同时加载。所有资源路径相对本 skill 安装目录（dsh 的 `resourceBase`），不是用户项目根目录。

## 要完成什么

帮用户解释、推导或构造能解决当前研究问题的机制。数学材料服务于这个任务，不能以选择透镜、列术语、生成知识卡或给模板命名作为交付。

- 用户已有目标、方案与约束时沿用；只有会改变结论的缺口才澄清，能带明确假设继续的就推进。
- 区分已有定理、当前推导、建模假设和经验结果；每个“最优、稳定、安全、等价”都要有相应条件。
- 用户要求实现/验证且环境允许时，执行已授权的最小检查并据观察修改候选。只要求概念或计划时按该范围交付。

## 先按任务分流

数学正确性、复杂度、收敛、稳定性或密码学安全性是目标时，即使任务含代码也介入。仅修 import/shape/OOM、重构、调参、部署或一般代码质量属于 E 纯工程，不加载研究材料。直接点名本 skill 可跳过自动触发判断，仍尊重纯工程边界。

| 场景 | 需要交付 | 材料怎么用 |
|---|---|---|
| A 分析 | 当前论点是否成立、关键原因与修法 | 从已有对象出发；相关锚点或透镜，不先做全套诊断 |
| B 跨域探索 | 找到有价值、结构上不同的数学思路 | 先读 [结构迁移](references/structural-transfer.md)，不以现有原型限定候选 |
| B 构造 | 将选定思路导出机制，说明实现与验证 | 需要落实数学化与实现时，读 [构造工作台](references/design-workbench.md) |
| C 查询 | 定义、直觉、公式与适用边界 | 通常一张锚点；不追加设计/GPU/实验报告 |
| D 验证 | 推导、条件界或最小反例 | 通常 1–2 锚点；用户要求运行时完成检查 |
| E 工程 | 按工程任务处理 | 不加载本系统材料 |

用户希望跨数学领域启发或减少模板化时，先去掉应用名字，寻找当前方案未满足的结构关系。沿不同数学表述发现候选，展示准确对象对应与能迁回的结论；不可能性和信息不足也是有效研究见解。生成阶段先不读设计原型，待关键映射成立后再查作对照。数学构造动作中的五例同样不是候选清单边界。

B 构造场景再确定希望改变的行为与决策时可用的信息，选择数学对象。构造工作台提供从对象到算子、落实定理假设、通过小验证取舍的动作。[数学构造动作](references/construction-moves.md) 按相关一节选读，可产生现有 22 个设计原型以外的机制；已有原型只作参照。

## Domain Router

按目标对象与所求保证判域，不能只数关键词。

| 域 | 可用材料 | 边界 |
|---|---|---|
| 共用数学 | `lenses/` 与 8 个非密码域的 37 张锚点 | 不自动加载 AI 原型或密码书稿 |
| AI 研究 | 共用数学、构造工作台、相关 AI 原型 | 不加载密码学锚点/书稿 |
| 密码学 | `knowledge-base/cryptography/` 的相关锚点；共用数学按结构需要补充 | 不加载 AI 原型/构造动作，不以 GPU 清单判断安全 |
| AI×密码 | 同时涉及密码学原语/形式安全性质与 AI 对象，且要求迁移或组合 | 只读交叉点所需材料 |

`hashing`、`attack`、`certificate`、一般证明或对抗优化不足以判密码域；特征哈希、Lipschitz 鲁棒性证书、随机平滑认证通常仍是 AI。统计隐私定义本身也不等于密码学。

交叉问题说明一次：原语及安全性质、AI 对象及功能、迁移方向、迁移后的假设是否可满足。纯密码任务先写攻击者能力、安全游戏、优势和归约资源；实现注意事项按具体方案选择。只有用户问密码实现性能时才分析相关硬件成本。

## 只读会改变当前决定的材料

- 已知所需数学结构时直接读锚点，不必先过透镜或索引。概念/短验证一般只需 1–2 份材料；构造任务可从工作台加 1–3 份相关资源起步，推导与验证确有需要再扩展。
- 透镜用于发现形式化方式，不是每次必走步骤；文件名不足以定位时才读 [索引](references/skill-index.md)。已有上下文足够时不重复读取。
- 锚点先读定义、核心公式及影响结论的风险/条件；设计原型已重复的数学段可以跳过。文件短或工具不支持分节时可读单文件。
- 卡片不足、需核对精确条件或用户要求来源时才读书稿/原论文。构造工作台、完整 critic 与 GPU 清单都不是所有任务的常驻上下文。
- 不展示内部文件加载路径或流程标签，除非用户在调试 skill；不复述材料，不把简单问题写成全面审查。

## 从求解到完成

**分析或验证**：提取对象、量词和假设，完成所需推导或构造反例。指出失败时，给保持用户目标的最小修正。结论是否适用于输出/任务，需要另行证明，不能从中间张量的性质直接跳过去。

**构造**：从期望行为与失败情形得到可检验关系，推导更新式、目标函数、求解器或决策规则。把定理变量代入实际张量，并处理关键假设：由结构强制、由求解残差控制，或只作为经验假设。训练时 teacher 的信息不能免费变成推理决策前输入。完成候选后才检查相关原型，避免过早把问题塞进已有模板。

**实现与验证**：写清前向、训练信号、梯度/求解路径和主要成本。已要求执行且有条件时，为当前候选生成并运行可解析小例、朴素基线或关键假设失效对照；观察失败后修订再检查。没有运行的实验只写计划，有限数值支持不能替代全称证明。硬件深查按需读 [GPU 清单](references/gpu-friendly-math.md)，只报告会改变选择的维度。

**持续研究**：沿用已有对象、条件、失败候选和实测结果，处理本轮新增证据；不重新启动整个路由。多轮候选/实验推进时读 [研究循环](references/agentic-workflow.md)。

## Knowledge Gap Protocol

现有锚点不够时，不要强行套卡：定位具体缺失的概念/定理，按需用透镜寻找结构，核验来源，写下足够完成本题的最小定义与条件，再回到求解或构造。临时卡注明 domain、来源、已知/推导/假设、未核验部分；仅频繁复用时才提议升级正式卡。详细记录字段见 [索引](references/skill-index.md)。

精确常数、定理条件、归约及新近方法优先核对原始论文或作者教材。蒸馏稿和模型记忆不是独立核验。PDF 只在当前仓库或用户已提供的位置按需查找；不可得时查一手网络来源或保留未核验状态，不虚构页码和引用。提出机制不代表已经证明论文新颖性。

## 交付前检查

先回答用户要的结论或主方案，再给足以复现和判断的依据。检查：关键条件是否落实，信息何时可得，近似改变了什么结论，是否存在退化解，以及所报告实验是否真正运行。短问题只保留影响答案的内容。

需要完整审查时再读 [math-critic](agents/math-critic.md)。需要具体例子时，只选当前域的一例：[低秩与 query](references/worked-examples/query-aware-compression.md)、[等变性](references/worked-examples/equivariance-check.md)、[PRF→MAC](references/worked-examples/security-reduction.md)。

资源入口：`lenses/`（15 个视角），`knowledge-base/`（41 张锚点，含 4 张密码卡），`design-patterns/`（22 个 AI 原型），`references/books/`（按需书稿）。这些是开放的起点，不是可构造机制的清单边界。
